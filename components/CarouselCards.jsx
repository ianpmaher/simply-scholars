import {
    motion,
    useMotionValue,
    useTransform,
    easeInOut,
} from "framer-motion";
import Image from "next/image";
import { useState } from "react";

const CarouselCards = (props) => {
}